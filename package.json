{
  "name": "site-eduardojung",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "db:migrate": "node --env-file-if-exists=.env.local scripts/migrate.js",
    "admin:create": "node --env-file-if-exists=.env.local scripts/provision-admin.js",
    "db:cleanup": "node --env-file-if-exists=.env.local scripts/cleanup.js",
    "dev": "node --env-file-if-exists=.env.local node_modules/vite/bin/vite.js",
    "build": "vite build",
    "lint": "oxlint",
    "preview": "vite preview",
    "start": "node --env-file-if-exists=.env.local server.js"
  },
  "dependencies": {
    "lucide-react": "^1.40.0",
    "pg": "^8.23.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.1.0",
    "oxlint": "^1.79.0",
    "vite": "^8.2.2"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
